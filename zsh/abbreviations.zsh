# chekc abbr_reset and abbr_clear_all in functions.zsh

abbr -g "aenv"="source .venv/bin/activate"
abbr -g "d"="dnf"
abbr -g "dnf i"="dnf install -y"
abbr -g "dnf r"="dnf remove -y"
abbr -g "dnf u"="dnf update -y"
abbr -g "dnf up"="dnf update -y"
abbr -g "dnf in"="dnf install -y"
abbr -g "dnf re"="dnf remove -y"
abbr -g "dnf se"="dnf search"
abbr -g "dni"="dnf install -y"
abbr -g "dnr"="dnf remove -y"
abbr -g "dnu"="dnf update -y"
abbr -g "do"="docker"
abbr -g "doc"="docker-compose"
abbr -g "docker c"="docker compose"
abbr -g "docker-compose b"="docker-compose build"
abbr -g "docker compose d"="docker compose down"
abbr -g "docker-compose d"="docker-compose down"
abbr -g "docker compose l"="docker compose logs"
abbr -g "docker-compose l"="docker-compose logs"
abbr -g "docker compose r"="docker compose restart"
abbr -g "docker-compose r"="docker-compose restart"
abbr -g "docker compose u"="docker compose up -d"
abbr -g "docker-compose u"="docker-compose up -d"
abbr -g "g"="git"
abbr -g "gg"="| grep -i"
abbr -g "git c"="git clone"
abbr -g "git co"="git checkout"
abbr -g "gic"="git clone"
abbr -g "git p"="git commit -av && git push"
abbr -g "gip"="git commit -av && git push"
abbr -g "git s"="git status"
abbr -g "la"="ls -lA"
abbr -g "ll"="ls -l"
abbr -g "md"="mkdir -p"
abbr -g "penv"="python -m venv .venv && source .venv/bin/activate"
abbr -g "s"="sudo"
abbr -g "sy"="systemctl"
abbr -g "sudo sy"="sudo systemctl"
abbr -g "sudo systemctl s"="sudo systemctl start"
abbr -g "sudo systemctl st"="sudo systemctl status"
abbr -g "sudo systemctl sto"="sudo systemctl stop"
abbr -g "sudo systemctl r"="sudo systemctl restart"
abbr -g "sudo systemctl en"="sudo systemctl enable"
abbr -g "sudo systemctl dis"="sudo systemctl disable"


